# Functional Tic Tac Toe

Incomplete. Still have spots where I am mutating data and a lot of the game logic is incomplete.

```
$ git clone
$ npm init
$ node index.js
```

To quit:

```
$ ps ax | grep node
$ kill #(where # matches node index.js)
```
